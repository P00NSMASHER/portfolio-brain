{
  "schema_version": "1.0.0",
  "engine_id": "portfolio-bounded-action-engine-v1",
  "mode": "BOUNDED_CHANNEL_EXECUTION",
  "enabled": true,
  "authority_class": "ACT",
  "allowed_project_ids": [
    "PRJ-001",
    "PRJ-002",
    "PRJ-003",
    "PRJ-004"
  ],
  "allowed_actions": {
    "CUSTOMER_EMAIL": {
      "allowed_consequences": [
        "LOW",
        "MEDIUM"
      ],
      "max_per_utc_day": 25,
      "max_per_recipient_per_utc_day": 1,
      "min_seconds_between_same_recipient": 3600,
      "max_subject_chars": 200,
      "max_body_chars": 12000,
      "credential_env": {
        "host": "PORTFOLIO_EMAIL_SMTP_HOST",
        "port": "PORTFOLIO_EMAIL_SMTP_PORT",
        "username": "PORTFOLIO_EMAIL_SMTP_USER",
        "password": "PORTFOLIO_EMAIL_SMTP_PASSWORD",
        "sender": "PORTFOLIO_EMAIL_FROM"
      }
    }
  },
  "prohibited_action_types": [
    "PAYMENT_OR_PURCHASE",
    "BILLING_CHANGE",
    "MOVE_MONEY",
    "LEGAL_OR_REGULATORY_COMMUNICATION",
    "CLAIM_OR_DISPUTE_SUBMISSION",
    "DESTRUCTIVE_DATA_DELETION",
    "PRIVATE_INFORMATION_DISCLOSURE",
    "LIVE_MARKET_TRADING",
    "BROKERAGE_ORDER",
    "AUTONOMOUS_INVESTMENT_POSITION",
    "CONSEQUENTIAL_CHILD_FACING_CHANGE"
  ],
  "state_persistence": {
    "mode": "GITHUB_ACTIONS_ARTIFACT",
    "artifact_name": "portfolio-action-engine-state",
    "retention_days": 30,
    "seed_file": "action_engine/ACTION_STATE_SEED.json",
    "sanitized_only": true
  },
  "kill_switches": {
    "repository_variable": "PORTFOLIO_ACTIONS_DISABLED",
    "file": "action_engine/KILL_SWITCH.json"
  },
  "retry_limit": 2,
  "invariants": [
    "Only explicitly allowlisted project/action pairs may execute.",
    "Raw recipients, subjects and message bodies are never persisted in durable action state.",
    "Every action has a deterministic idempotency key and duplicate execution is suppressed.",
    "Daily and per-recipient rate limits are checked before transport invocation.",
    "Missing channel credentials block execution before any side effect.",
    "Action receipts store hashes, counters, timestamps, action IDs and remote references only.",
    "Payments, money movement, legal/regulatory communications, claims/disputes, destructive deletion, private disclosure, live trading/broker execution and consequential child-facing changes are outside this executor.",
    "The action kill switch overrides all allowlists."
  ]
}
