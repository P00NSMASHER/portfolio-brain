{
  "schema_version": "1.0.0",
  "engine_id": "portfolio-bounded-action-engine-v1",
  "mode": "CHATGPT_GMAIL_CONNECTOR_GATEWAY",
  "enabled": true,
  "authority_class": "ACT",
  "allowed_project_ids": [
    "PRJ-001",
    "PRJ-002",
    "PRJ-003",
    "PRJ-004"
  ],
  "allowed_actions": {
    "CUSTOMER_EMAIL": {
      "allowed_consequences": [
        "LOW",
        "MEDIUM"
      ],
      "max_per_utc_day": 25,
      "max_per_recipient_per_utc_day": 1,
      "min_seconds_between_same_recipient": 3600,
      "max_subject_chars": 200,
      "max_body_chars": 12000,
      "private_queue": {
        "mode": "GMAIL_DRAFT",
        "internal_subject_prefix": "[PB:",
        "planner_label": "PortfolioBrain/Ready",
        "sent_label": "PortfolioBrain/Sent"
      }
    }
  },
  "prohibited_action_types": [
    "PAYMENT_OR_PURCHASE",
    "BILLING_CHANGE",
    "MOVE_MONEY",
    "LEGAL_OR_REGULATORY_COMMUNICATION",
    "CLAIM_OR_DISPUTE_SUBMISSION",
    "DESTRUCTIVE_DATA_DELETION",
    "PRIVATE_INFORMATION_DISCLOSURE",
    "LIVE_MARKET_TRADING",
    "BROKERAGE_ORDER",
    "AUTONOMOUS_INVESTMENT_POSITION",
    "CONSEQUENTIAL_CHILD_FACING_CHANGE"
  ],
  "state_persistence": {
    "mode": "SANITIZED_GITHUB_LEDGER_PLUS_GMAIL_SENT_SOURCE_OF_TRUTH",
    "ledger_file": "action_engine/GMAIL_GATEWAY_LEDGER.json",
    "gmail_sent_label": "PortfolioBrain/Sent",
    "sanitized_only": true
  },
  "kill_switches": {
    "file": "action_engine/KILL_SWITCH.json"
  },
  "retry_limit": 2,
  "invariants": [
    "Only explicitly allowlisted project/action pairs may execute.",
    "Raw recipients, subjects and message bodies are never persisted in durable action state.",
    "Every action has a deterministic idempotency key and duplicate execution is suppressed.",
    "Daily and per-recipient rate limits are checked against Gmail/Sanitized ledger state before Gmail send.",
    "The authenticated Gmail connector profile is verified privately by the scheduled gateway before any Gmail mutation; the public repo stores only the PRIMARY_GMAIL_CONNECTOR alias.",
    "GitHub action receipts store hashes, counters, timestamps and action IDs only; Gmail message/thread IDs are hashed before persistence.",
    "Payments, money movement, legal/regulatory communications, claims/disputes, destructive deletion, private disclosure, live trading/broker execution and consequential child-facing changes are outside this executor.",
    "The action kill switch overrides all allowlists.",
    "Raw recipients, draft bodies, subjects, Gmail message IDs and Gmail thread IDs remain inside the connected Gmail account and ChatGPT connector execution context.",
    "GitHub Actions contains no SMTP transport or Gmail credential path."
  ],
  "execution_provider": "CHATGPT_GMAIL_CONNECTOR",
  "gmail_account_ref": "PRIMARY_GMAIL_CONNECTOR"
}
